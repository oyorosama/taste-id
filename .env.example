# TasteID Environment Variables
# Copy this file to .env.local and fill in your values

# =============================================
# DATABASE (Vercel Postgres)
# =============================================
# Get these from your Vercel dashboard after creating a Postgres database
POSTGRES_PRISMA_URL="postgresql://user:password@host:5432/database?sslmode=require&pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host:5432/database?sslmode=require"

# =============================================
# AUTHENTICATION (NextAuth.js)
# =============================================
NEXTAUTH_URL="http://localhost:3000"
AUTH_SECRET="generate-a-secure-secret-here"

# GitHub OAuth (optional)
GITHUB_ID=""
GITHUB_SECRET=""

# Google OAuth (optional)
GOOGLE_ID=""
GOOGLE_SECRET=""

# =============================================
# API KEYS
# =============================================

# TMDB Read Access Token (safe for client-side - read-only)
# Get from: https://www.themoviedb.org/settings/api
NEXT_PUBLIC_TMDB_READ_ACCESS_TOKEN=""

# AniList: FREE, NO AUTH REQUIRED
# Direct GraphQL queries work without authentication

# RAWG (server-side and client-side - read-only)
# Get from: https://rawg.io/apidocs (free tier: 20,000 requests/month)
NEXT_PUBLIC_RAWG_API_KEY=""

# =============================================
# SECURITY NOTES
# =============================================
# - NEXT_PUBLIC_* variables ARE exposed to the browser
# - Only TMDB uses NEXT_PUBLIC_* because it's a read-only token
# - IGDB credentials are server-side only (no NEXT_PUBLIC prefix)
# - AniList doesn't require any credentials for search
